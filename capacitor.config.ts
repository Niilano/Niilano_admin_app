import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Niilano_admin_app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
